(function(window) {
  // Generated by uRequire v0.3.0alpha15
define(['require', 'underscore', './C'], 
  function (require, _, C) {var isWeb = (typeof define === 'function' && define.amd), isNode = !isWeb;

// uRequire: start body of original AMD module
console.log(C);
    return _.map("qwer,asdf,zxvc".split(), function(word, i) {
        return "[" + i + "] " + word;
    });
// uRequire: end body of original AMD module


});
})(__global);